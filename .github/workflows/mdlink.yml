---
name: Check Markdown Links

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  markdown-link-check:
    if: ${{ github.repository == 'pacroy/.github' }}
    name: Check Markdown Links
    uses: ./.github/workflows/wf_mdlink.yml
